[project]
name = "automation"
version = "0.1.0"
description = "A basic automation test using playwright and pytest"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "allure-pytest>=2.15.3",
    "playwright>=1.58.0",
    "pytest>=9.0.2",
]
